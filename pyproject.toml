[project]
name = "discrete-event-simulation"
version = "0.1.0"
description = "Discrete simulation framework (discrete-event and discrete-rate style models)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "matplotlib>=3.8.0",
    "networkx>=3.2",
]
